{
  "name": "amfi-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal deployable web app scaffold for Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
